(function ($) {
    var user_id = <%= @other_user.id %>,
        timeout = setTimeout(function () {
            $.get(Routes.who_to_follow_path({format: 'js'}));
        }, 2000);

    $('.js-follow-' + user_id).replaceWith('<%= j render('users/button_unfollow', user: @other_user) %>');

    $(document).on('click', '.js-unfollow-' + user_id, function () {
        clearTimeout(timeout);
    });
})(jQuery);